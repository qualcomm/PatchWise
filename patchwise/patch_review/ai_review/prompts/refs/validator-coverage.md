# Validator Coverage Matrix

Maps each structural validator check that `scripts/validate_review_packet.py` enforces to the rule family it covers, the artifacts it inspects, the governing rule reference, and the repair action exposed to the daemon repair prompt.

This matrix maps each structural validator check to the rule family it enforces, the artifacts it inspects, the governing rule reference, and the repair action exposed to the daemon repair prompt. The Mode column is derived from the live dispatch wiring (`validator_manifest()`), so it cannot drift from what the validator actually runs.

Total checks: 88

## Category Summary

| Category | Checks |
|---|---:|
| architecture | 1 |
| dt | 11 |
| dt-binding | 1 |
| evidence | 8 |
| hardware | 22 |
| helper/refactor | 21 |
| prompt/packet integrity | 3 |
| rendering | 1 |
| report-structure | 2 |
| rule card enforcement | 2 |
| severity/gate | 6 |
| structure | 8 |
| style/contracts | 2 |

## Mode Summary

| Mode | Checks |
|---|---:|
| report-only | 26 |
| source-aware | 48 |
| structural | 14 |

## Matrix

| Check | Mode | Category | Artifacts | Rule ref | Repair action |
|---|---|---|---|---|---|
| `gate_trace` | report-only | severity/gate | html_report | gate-rules.md#THREE-GATE RULE — FINDING VALIDATION, MANDATORY, SEQUENTIAL, NON-BYPASSABLE | Add the Gate 1/Gate 2/Gate 3 trace (or 'Always-BUG exception:') inside each non-NIT finding-card .body; NIT cards need a 'Style track:' line. Re-check reachability/harm/severity before writing the trace — do not fabricate it. |
| `step_record` | report-only | structure | html_report, step_completion_record | core.md#Your procedure — 7 mandatory steps | Restore the per-block &lt;!-- STEP_COMPLETION_RECORD --&gt; with all mandatory step_* fields marked DONE; only mark a step DONE when the underlying work was actually performed. |
| `conditional_sections` | report-only | structure | html_report, packet_file | core.md#Your procedure — 7 mandatory steps | Add the missing conditional section header (DT / DT-driver / Hardware Engineering) the patch triggers; the review packet lists which are required for this patch. |
| `anchor_id` | report-only | structure | html_report | orchestrator-workflow.md#Step 6 | Give every per-commit finding-card id="patch-&lt;N&gt;-finding-&lt;K&gt;" with 1-based N and K. |
| `banner_dedup` | structural | structure | html_report | orchestrator-workflow.md#Step 6 | Each canonical per-commit finding must appear once in the banner; remove duplicate banner cards and keep a single anchor link per finding. |
| `banner_consistency` | structural | structure | html_report | orchestrator-workflow.md#Step 6 | Reconcile banner [BUG]/[CONCERN]/[MINOR] cards and stat-chip counts to exactly match the per-commit blocks (Step 6.6). |
| `verdict_counts_consistency` | structural | structure | html_report | orchestrator-workflow.md#Step 6 | Make the verdict pill match the bug/concern stat-chip counts: READY TO APPLY requires 0 bugs AND 0 concerns; NEEDS DISCUSSION requires 0 bugs AND &gt;=1 concerns; NEEDS FIXES requires &gt;=1 bugs. Either escalate/downgrade the pill or add/remove the missing finding-card (Step 6.6). |
| `render_format` | report-only | rendering | html_report | html-template.md#Finding-card render-safe pattern | Keep .body, .file-ref, and .suggestion as prose/inline HTML only. Move &lt;pre&gt;, lists, tables, headings, and nested &lt;div&gt; blocks to sibling elements; keep Gate traces in .body. |
| `pre_existing_scope` | report-only | structure | html_report | core.md#Your procedure — 7 mandatory steps | Move pre-existing-only issues out of .finding-card and verdict-banner summaries; keep them as patch-local pre-existing notes so they do not affect verdict/banner/stats. Add data-attribution="introduced" or "newly_exposed" to real finding cards; if the patch makes a pre-existing path newly reachable or worse, state the before-vs-after reachability delta in the card instead. |
| `block_fragment` | structural | structure | html_report, block_file | core.md#Your procedure — 7 mandatory steps | A block fragment must contain exactly one .commit-block ending with &lt;/div&gt;&lt;!-- /commit-block --&gt;. |
| `build_break_order` | structural | severity/gate | html_report, tests_file, build_output | gate-rules.md#Step 4 — Review Each Commit | When the W=1 build failed because of the patch, the build-break finding must be the FIRST finding-card in that block (and its banner card first). If the failure is proven pre-existing, keep that proof as a non-finding build/test note instead. |
| `build_artifact_validity` | structural | evidence | tests_file, build_output | startup-workflow.md#Step 2 | Replace any interactive-Kconfig prompt log with a real non-interactive W=1 build artifact; rerun run_w1_build.py. |
| `hardware_trigger_consistency` | report-only | hardware | html_report, patch_corpus, packet_file | hardware-eng.md#Step 3f — Hardware Engineering Perspective | A patch that touches registers/probe/remove/PM/IRQ/DMA cannot mark the Hardware Engineering section N/A — add the real HW analysis. |
| `test_results_build_notes_consistency` | report-only | report-structure | html_report | output-format-mini.md#HTML Block Contract | The Test Results card and per-commit Build/Test Notes disagree about whether Build (W=1) passed. Decide one way: if the compile actually succeeded and the FAIL row was triggered by a benign Kconfig syncconfig restart, change the row to PASS and put the syncconfig explanation in the Notes cell; if it really failed, quote the compiler error in the Notes cell so the FAIL is self-evident. |
| `test_results_fail_evidence` | report-only | report-structure | html_report | html-template.md#HTML skeleton | When a Test Results row is FAIL, the Notes cell MUST carry concrete evidence: either quote a compiler/checker diagnostic (e.g. \`error: implicit declaration of function ...\`, \`WARNING: line over 100 characters\`) directly in the cell, OR include an explicit pointer like \`see Patch N — Build / Test Notes\` (or an anchor link \`&lt;a href="#patch-N-..."&gt;\`). A bare count like 'failure(s): patch 1' is not actionable. |
| `refactor_coverage` | report-only | helper/refactor | html_report, patch_corpus | code-logic-interaction.md#3c.4 Interaction Picture | A rate/performance abstraction refactor must cover every alternate path (DMA/GPI). Add the missing path to the coverage analysis or raise a finding. |
| `future_risk_gate` | report-only | severity/gate | html_report | gate-rules.md#THREE-GATE RULE — FINDING VALIDATION, MANDATORY, SEQUENTIAL, NON-BYPASSABLE | A row in the current-safe/future-risk table must not be emitted as a [CONCERN]; downgrade to a note or prove a present-tree harm. |
| `safe_clearance_gate` | report-only | severity/gate | html_report | gate-rules.md#THREE-GATE RULE — FINDING VALIDATION, MANDATORY, SEQUENTIAL, NON-BYPASSABLE | Remove safe/no-action, self-negating, or process-only notes that were emitted as findings; a cleared or informational item is not a finding-card. |
| `platform_enablement_ready_to_apply` | report-only | severity/gate | html_report, patch_corpus | gate-rules.md#Step 4 — Review Each Commit | Do not leave a platform-enablement / add-support series at READY TO APPLY without explicit lifecycle cleanup, compatibility fallback, and selector/cardinality audit proof in the report. |
| `severity_crash_floor` | report-only | severity/gate | html_report, patch_corpus | gate-rules.md#THREE-GATE RULE — FINDING VALIDATION, MANDATORY, SEQUENTIAL, NON-BYPASSABLE | A finding describing a crash/NULL-deref/UAF must be at least [CONCERN]; raise the severity or correct the description. |
| `severity_restore_floor` | report-only | hardware | html_report, patch_corpus | hardware-eng-pm-register-access.md#3f.1 Device Power State Before Register Access | A finding describing a dropped suspend/resume restore must be at least [CONCERN]; raise the severity or prove the restore happens. |
| `codebase_audit_record` | report-only | evidence | html_report, step_completion_record | startup-workflow.md#Step 2 | Add the codebase_audit: line to the STEP_COMPLETION_RECORD with entrypoints=/callees=/siblings=/files=[...] for code patches. |
| `codebase_audit_required` | report-only | evidence | html_report, patch_corpus, step_completion_record | startup-workflow.md#Step 2 | Add the three 'codebase audit: entrypoints/callees/siblings' lines, the state/lifecycle workflow line, and the control-flow/data-flow/before-vs-after delta lines to the Code Logic Maps for every packet-mode block. For DTS/YAML-only patches, map entrypoints/callees/siblings to binding consumers, parent/sibling DTSI/DTS files, schema refs, or subsystem readers instead of writing N/A. |
| `on_demand_reads_record` | report-only | evidence | html_report, step_completion_record | startup-workflow.md#Step 2 | Record each targeted source read as 'on-demand read: &lt;path&gt; — &lt;reason&gt;' in the Code Logic Maps. |
| `inconclusive_requires_read_attempt` | report-only | evidence | html_report, step_completion_record | startup-workflow.md#Step 2 | Before downgrading a finding to inconclusive or claiming equivalence/safety, record one targeted on-demand read of the file that holds the needed fact. |
| `evidence_manifest_record` | structural | evidence | html_report, evidence_manifest, step_completion_record | startup-workflow.md#Step 2 | Add 'evidence_manifest: DONE path=&lt;tmp/evidence/patch_N_evidence.json&gt;' to the STEP_COMPLETION_RECORD. |
| `evidence_required_reads` | structural | evidence | html_report, evidence_manifest, step_completion_record | startup-workflow.md#Step 2 | The evidence manifest marks required reads (helper bodies, sibling paths, parent schemas) that the block must show were read; read them or raise a finding instead of clearing. |
| `prompt_artifact` | structural | prompt/packet integrity | prompt_file | orchestrator-workflow.md#Per-Patch Reviewer Subagent | Ensure the saved per-patch prompt (tmp/patch_N_prompt.md) exists and lists every required input file. Re-write it before re-spawn. |
| `packet_artifact` | structural | prompt/packet integrity | packet_file | startup-workflow.md#Step 2 | Ensure the compact reviewer packet (tmp/patch_N_review_packet.md) exists and passes scripts/validate_review_packet.py for this patch. |
| `rule_card_attestation` | structural | rule card enforcement | packet_file, html_report | rule-cards/runtime-pm-bracket-safety.md#Mandatory Attestation Record | When a rule card with a mandatory attestation record fires, the subagent must produce the corresponding attestation record in its Code Logic Maps or DT/DT-Binding Notes. Re-spawn the subagent with a note requiring the specific attestation record format. |
| `rule_card_coverage` | structural | rule card enforcement | packet_file, html_report, step_completion_record | output-format-mini.md#HTML Block Contract | Add a Rule Card Coverage section and a rule_card_coverage: STEP_COMPLETION_RECORD line that name every selected card from the packet JSON as checked, finding, or inconclusive. If the packet has focused_review_obligations, visibly disposition every obligation ID as FINDING, SAFE, or INCONCLUSIVE before claiming No issues found. |
| `hardware_notes_specificity` | report-only | hardware | html_report, step_completion_record | output-format-mini.md#HTML Block Contract | When step_3f_hardware_eng is DONE, replace generic hardware notes with concrete evidence from the patch/context: thermal trip values, hysteresis, #cooling-cells, tmd-names, provider/consumer IDs, QMI instance IDs, PM/IRQ/DMA/clock/regulator facts, or an explicit non-applicability rationale. |
| `runtime_override_artifact` | structural | prompt/packet integrity | runtime_config, sparse_file, html_report | startup-workflow.md#Step 3 | Honor the daemon/runtime override artifact exactly: if sparse is disabled, keep the sparse artifact as \`(sparse disabled by config)\` and report sparse as SKIP/disabled-by-config instead of running it. |
| `source_corpus_required` | structural | evidence | patch_corpus, source_root | orchestrator-workflow.md#Step 6 | Provide the patch diff/corpus (tmp/review_patches or the patch diff) so source-aware checks can run; do not downgrade to HTML-only validation. |
| `binding_parent_compatible_consistency_source_aware` | source-aware | dt | html_report, patch_corpus | dt-binding-yaml.md#3d.1 DT-Binding Schema (\`.yaml\`) Rules | Ensure the child binding's compatible is consistent with the parent wrapper schema's allowed values in the source tree. |
| `old_dtb_compatibility_source_aware` | source-aware | dt | html_report, patch_corpus | dt-binding-yaml.md#3d.1 DT-Binding Schema (\`.yaml\`) Rules | When a binding makes resources newly required, state whether an old DTB still probes with the new kernel and why; otherwise raise a compatibility finding. |
| `dt_fallback_old_kernel_new_dtb_source_aware` | source-aware | dt | html_report, patch_corpus | rule-cards/dt-compatible-fallback-contract.md | For a new compatible added with an existing fallback, prove the old-kernel/new-DTB fallback descriptor is safe for any new resources, clocks, register offsets, and sequencing, or file the unsafe fallback finding. |
| `provider_cells_const_source_aware` | source-aware | dt | html_report, patch_corpus | dt-binding-yaml.md#3d.1 DT-Binding Schema (\`.yaml\`) Rules | When adding provider #*-cells properties, define the fixed cell count with const: for the compatible or explicitly prove variable cell counts are valid. |
| `optional_clk_dead_enoent_fallback` | source-aware | dt | html_report, patch_corpus | dt-driver.md#Step 3d.3 — Driver \`of_match\` & \`of_*\` API Consistency | Do not keep -ENOENT fallback logic after switching to devm_clk_bulk_get_optional(); optional getters succeed when the optional clock is absent, so flag the dead branch or use a required getter. |
| `required_clk_bulk_zero_count_source_aware` | source-aware | dt | html_report, patch_corpus | dt-driver.md#Step 3d.3 — Driver \`of_match\` & \`of_*\` API Consistency | For compatible-required clocks acquired with devm_clk_bulk_get_all(), handle both negative errors and a zero returned clock count before hardware access. |
| `framework_status_callback_power_state_source_aware` | source-aware | hardware | html_report, patch_corpus | rule-cards/framework-status-callback-power-state.md | When a regulator/framework status callback reads MMIO in a clock-gated resource path, prove that the status register is accessible before enable or file the unpowered-read finding. |
| `framework_status_bootloader_refcount_source_aware` | source-aware | hardware | html_report, patch_corpus | rule-cards/framework-status-callback-power-state.md | When a framework status callback can report hardware already enabled, prove Linux-side clock/PM counts are synchronized before any later disable/unprepare path, or file the underflow/unbalanced-disable finding. |
| `managed_device_link_manual_remove_source_aware` | source-aware | helper/refactor | html_report, patch_corpus | code-logic-alloc-release.md#API alloc/release-pairing contract checklist | Do not pair DL_FLAG_AUTOREMOVE_* device links with manual device_link_remove()/device_link_del() cleanup unless the driver-core contract explicitly allows that exact state. |
| `retained_dynamic_object_cleanup_source_aware` | source-aware | helper/refactor | html_report, patch_corpus | code-logic.md#3c.2 Data-Flow Picture | When a static/global descriptor retains a dynamically allocated framework object, every cleanup/error path that frees it must clear the retained pointer or prove retry/rebind cannot reuse freed state. |
| `level_irq_reenable_without_clear_source_aware` | source-aware | hardware | html_report, patch_corpus | hardware-eng-irq-dma-context.md#3f.5 Interrupt and DMA Context Constraints | When an IRQ path exits early on runtime-PM/device-state checks, clear the asserted source or leave it masked before enable_irq(); otherwise flag the level-triggered interrupt-storm risk. |
| `alternate_path_state_reset_source_aware` | source-aware | hardware | html_report, patch_corpus, source_root | code-logic-state-machine.md#3c.3 State-Machine / Lifecycle Picture | For mode/type/selector/config fields set on one path, prove each alternate path (TPG, loopback, internal source) resets or guards the field, or file the stale-state finding. |
| `unvalidated_arithmetic_input_source_aware` | source-aware | helper/refactor | html_report, patch_corpus, source_root | code-logic.md#3c.2 Data-Flow Picture | For GENMASK(count - 1), division, shift, or arithmetic fed by a zero-capable DT/variant value, prove bounds validation rejects zero or file the degenerate-input finding. |
| `branch_precedence_regression_source_aware` | source-aware | helper/refactor | html_report, patch_corpus, source_root | code-logic.md#3c.1 Control-Flow Picture | Trace the widened/reordered if/else chain: name which input now lands in the earlier arm and prove the bypassed side effect still runs, or file the branch-diversion finding. |
| `branch_diversion_producer_coupling_source_aware` | source-aware | helper/refactor | html_report, patch_corpus, source_root | code-logic.md#3c.1 Control-Flow Picture | When the producer source shows connect-status and event/IRQ flags are independent, do not clear diversion by mutual-exclusion prose; file the finding or cite a producer line that truly couples them. |
| `readpath_widening_writer_locked_source_aware` | source-aware | hardware | html_report, patch_corpus, source_root | hardware-eng-resource-lifecycle.md#3f.2 Hardware Resource Lifecycle Symmetry | Name the writer free site and lock for the widened read window, or quote a real lifetime guarantee (shared lock, refcount, RCU, suppress_bind_attrs); otherwise file the UAF/race finding. |
| `binding_companion_dependency_source_aware` | source-aware | dt | html_report, patch_corpus | dt-binding-yaml.md#3d.1 DT-Binding Schema (\`.yaml\`) Rules | A property that requires a companion property (e.g. dmas needs dma-names) must include it; fix the binding/example or raise a finding. |
| `dma_names_example` | report-only | dt | html_report, patch_corpus | dt-binding-yaml.md#3d.1 DT-Binding Schema (\`.yaml\`) Rules | A binding example using 'dmas' must also list 'dma-names'; add it to the example. |
| `dma_names_source_aware` | source-aware | dt | html_report, patch_corpus, evidence_manifest | dt-binding-yaml.md#3d.1 DT-Binding Schema (\`.yaml\`) Rules | Verify dmas/dma-names pairing against the DTS/driver in the source tree; fix the mismatch or raise a finding. |
| `match_data_guard` | report-only | dt | html_report, patch_corpus | dt-driver.md#Step 3d.3 — Driver \`of_match\` & \`of_*\` API Consistency | Guard the device_get_match_data()/of_device_get_match_data() result against NULL before dereferencing, or raise a finding. |
| `match_data_source_aware` | source-aware | dt | html_report, patch_corpus, evidence_manifest | dt-driver.md#Step 3d.3 — Driver \`of_match\` & \`of_*\` API Consistency | Confirm in the source tree that the match-data result is NULL-checked on the touched path; fix or raise a finding. |
| `selector_cardinality_source_aware` | source-aware | helper/refactor | html_report, patch_corpus | code-logic.md#3c.2 Data-Flow Picture | For add-support / new-descriptor work, explicitly compare every selector/cardinality surface (IDs, counts, names, routes, provider arrays) or raise a mismatch finding. |
| `aggregate_per_element_scale_source_aware` | source-aware | helper/refactor | html_report, patch_corpus, source_root | code-logic.md#3c.2 Data-Flow Picture | When a per-element bandwidth/rate is divided by a width/size from the container (\`desc-&gt;\`/\`provider-&gt;\`/\`qp-&gt;desc-&gt;\`) and the element exposes its own same-dimension field, compute from the per-element field (then aggregate) or prove the set is homogeneous; otherwise raise the mis-scale finding. |
| `cross_instance_pointer_unbind_source_aware` | source-aware | hardware | html_report, patch_corpus, source_root | hardware-eng-resource-lifecycle.md#3f.2 Hardware Resource Lifecycle Symmetry | When a provider stores a cross-provider/peer raw node pointer, prove a lifetime guarantee against independent sysfs unbind (\`.suppress_bind_attrs = true\`, get_device/refcount, managed device_link, or coordinated teardown) or file the UAF finding. |
| `peer_dimension_admission_source_aware` | source-aware | helper/refactor | html_report, evidence_manifest | code-logic.md#3c.2 Data-Flow Picture | When an admission or capacity guard validates one dimension, check each peer dimension that shares the same contract or raise a finding for the missing peer-dimension guard. |
| `duplicate_cleanup_fallthrough_source_aware` | source-aware | helper/refactor | html_report, evidence_manifest | code-logic.md#3c.1 Control-Flow Picture | When an unwind path duplicates teardown/release work through fallthrough or shared labels, file the double-cleanup risk as a finding or prove the duplicate call sites are not both reachable. |
| `failed_start_stale_state_source_aware` | source-aware | hardware | html_report, evidence_manifest | code-logic-state-machine.md#3c.3 State-Machine / Lifecycle Picture | When a start/resume/enable/load path publishes success state before the operation can fail, clear that state on the failure edge or file the stale-state contamination as a finding. |
| `pm_runtime_get_sync_check` | report-only | hardware | html_report, patch_corpus | hardware-eng-pm-register-access.md#3f.1 Device Power State Before Register Access | A bare pm_runtime_get_sync() must check its return and pm_runtime_put on error; fix the bracket or raise a finding. |
| `device_unregister_pointer_hygiene` | report-only | hardware | html_report | hardware-eng-resource-lifecycle.md#3f.2 Hardware Resource Lifecycle Symmetry | When a block unregisters/put_device()s a caller-owned object (-&gt;fw_dev, drvdata, cached handle) and clears it as safe, quote the \`&lt;ptr&gt; = NULL\` reset line that runs after the unregister, an explicit no-escape statement, or file a stale-pointer [BUG]/[CONCERN]. A positive note is not a discharge. |
| `per_block_vote_scope` | report-only | hardware | html_report | hardware-eng-pm-register-access.md#3f.1 Device Power State Before Register Access | When a per-block/per-core helper drops a global OPP/perf/genpd/clock-rate vote and the review clears it as safe, trace the full multi-block sequence (name each sibling block and prove none stays active when the vote drops) or file a [BUG]/[CONCERN]. |
| `pm_get_sync_balance` | report-only | hardware | html_report | hardware-eng-pm-register-access.md#3f.1 Device Power State Before Register Access | A failed pm_runtime_get_sync() leaves the usage count incremented; the error edge must call pm_runtime_put_noidle() (or migrate to pm_runtime_resume_and_get). Do not clear a get_sync as correct while citing put_sync()/put() on the error path — quote the put_noidle line or file a [BUG]/[CONCERN]. |
| `pm_runtime_get_sync_source_aware` | source-aware | hardware | html_report, patch_corpus, evidence_manifest | hardware-eng-pm-register-access.md#3f.1 Device Power State Before Register Access | Inspect the touched source path: an unchecked pm_runtime_get_sync() requires review proof or a finding. |
| `clk_handle_ownership_source_aware` | source-aware | hardware | html_report, patch_corpus | hardware-eng-resource-lifecycle.md#3f.2 Hardware Resource Lifecycle Symmetry | A handle returned by a non-devm reference getter (e.g. of_clk_get()/of_clk_get_by_name()/clk_get(), regulator_get(), of_reset_control_get()) is an owned reference; require the matching *_put() on every error/unbind path, convert to the devm_* variant, or prove ownership is transferred. |
| `clk_enable_idempotency_source_aware` | source-aware | hardware | html_report, patch_corpus | hardware-eng-resource-lifecycle.md#3f.2 Hardware Resource Lifecycle Symmetry | For *_prepare_enable() / *_enable() in re-enterable callbacks (open/close, hw_params, set-rate, runtime-PM, recovery, stream restart), prove repeated calls cannot over-increment enable counts or file the missing idempotency/refcount finding. |
| `asoc_dai_target_source_aware` | source-aware | helper/refactor | html_report, patch_corpus | code-logic.md#3c.2 Data-Flow Picture | For role-typed endpoint helpers (currently detected: ASoC snd_soc_dai_set_*() picking cpu_dai vs codec_dai), validate the role the surrounding intent actually selects, including secondary arguments (clk_id/direction/index) and return handling; file a finding when the chosen role contradicts nearby flags, names, or commit text. |
| `non_alloc_enomem_source_aware` | source-aware | style/contracts | html_report, patch_corpus | coding-style.md#Error Paths & Resource Management | Match the errno to the failing operation: reserve -ENOMEM for genuine allocation failures and prefer -ENODEV/-EINVAL/-ENODATA/-ENOENT (or preserve the helper's own return) for absent match data, descriptors, or lookup results. |
| `pm_runtime_post_get_return_source_aware` | source-aware | hardware | html_report, patch_corpus | hardware-eng-pm-register-access.md#3f.1 Device Power State Before Register Access | After a successful pm_runtime_get_sync(), every newly added early return must route through pm_runtime_put*()/cleanup or prove it exits before the get succeeds. |
| `firmware_metadata_source_aware` | source-aware | hardware | html_report, patch_corpus | hardware-eng-resource-lifecycle.md#3f.2 Hardware Resource Lifecycle Symmetry | When a driver adds a new firmware path or hardware firmware directory, add matching MODULE_FIRMWARE() metadata or document why the path is inherited/unused. |
| `binding_compatible_conditional_source_aware` | source-aware | dt-binding | html_report, patch_corpus | dt-binding-yaml.md#Completeness checks | For each added binding compatible, reconcile existing allOf/if/then resource conditionals; add the compatible to the right conditional or prove generic constraints are sufficient. |
| `pm_runtime_positive_return_source_aware` | source-aware | hardware | html_report, patch_corpus | hardware-eng-pm-register-access.md#3f.1 Device Power State Before Register Access | Errno-style wrappers must not forward a &gt;=0-success helper return (currently detected: pm_runtime_put_sync()) without normalizing positive values to 0; clamp with \`ret &lt; 0 ? ret : 0\` / \`max(ret, 0)\` unless every caller documents and handles positive returns. |
| `printf_format_type_source_aware` | source-aware | style/contracts | html_report, patch_corpus | coding-style.md#Data Structures & Types | Compare each new printf-like format specifier with the argument type/signedness; use %u or an intentional cast for unsigned values instead of %d. |
| `relocated_teardown_step_source_aware` | source-aware | helper/refactor | html_report, patch_corpus | code-logic.md#3c.5 Before-vs-After Delta | The patch removed a teardown/bookkeeping step (list_del, free, put, NULL, decrement) from a helper that has multiple callers. Audit every caller: prove each re-performs the step or never needed it, or file the leak/double-free/UAF for the caller that now skips it. |
| `lock_coverage_symmetry_source_aware` | source-aware | helper/refactor | html_report, patch_corpus | code-logic-interaction.md#3c.4 Interaction Picture | A field is mutated under a lock on one path but set/reset/cleared without that lock on another. Audit every access to the field, take the same lock on the unlocked path, or prove the unlocked access is single-threaded/owner-private; otherwise file the data-race [CONCERN]/[BUG]. |
| `touched_unsafe_pm_source_aware` | source-aware | hardware | html_report, patch_corpus, source_root, evidence_manifest | hardware-eng-pm-register-access.md#3f.1 Device Power State Before Register Access | A touched source file with a bare pm_runtime_get_sync() needs either source-backed review proof or a recorded finding. |
| `resource_abstraction_bypass_source_aware` | source-aware | hardware | html_report, patch_corpus | hardware-eng-irq-dma-context.md#3f.5 Interrupt and DMA Context Constraints | Do not declare an alternate path safe while saying it bypasses a new resource/rate/power abstraction unless you prove it is unreachable or contract-compatible. |
| `core_table_vendor_entry_source_aware` | source-aware | architecture | html_report, patch_corpus | code-logic.md#3c.6 Subsystem Layering / Placement | When a patch threads a vendor/driver-specific construct into a core subsystem (core table entry, driver-private/vendor #include, or CONFIG_&lt;VENDOR&gt; guard in drivers/iommu, drivers/base, of/, kernel/, mm/), raise the layering question as a [CONCERN] and name the generic alternative, or state the placement is generic / maintainer-acknowledged. Do not clear it with a positive note. |
| `stack_struct_zero_init_source_aware` | source-aware | helper/refactor | html_report, patch_corpus | code-logic-wire-protocol.md#Wire protocol struct checklist | A stack struct passed by pointer to a kernel API (e.g. dev_pm_domain_attach_data -&gt; devm_pm_domain_attach_list) must be zero-initialized (= {} / memset) before field assignment, or the review must address the uninitialized-field hazard; otherwise file a [CONCERN]/[BUG]. |
| `pas_metadata_release_source_aware` | source-aware | helper/refactor | html_report, patch_corpus | code-logic-alloc-release.md#API alloc/release-pairing contract checklist | A firmware-load path calling qcom_mdt_pas_load()/qcom_scm_pas_init_image() must pair it with qcom_scm_pas_metadata_release() once authentication completes, or the review must flag the per-load metadata leak as a [BUG]. |
| `resource_helper_guard_source_aware` | source-aware | helper/refactor | html_report, patch_corpus | code-logic-pointer-api.md#Pointer-returning API call checklist | Show the resource helper's guard/precondition holds on the touched path in the source tree, or raise a finding. |
| `helper_replacement_postcondition_source_aware` | source-aware | helper/refactor | html_report, patch_corpus | code-logic-interaction.md#3c.4 Interaction Picture | Prove the replacement helper establishes every postcondition the original did (from the source tree), or raise a finding. |
| `helper_side_effect_source_aware` | source-aware | helper/refactor | html_report, patch_corpus | code-logic-interaction.md#3c.4 Interaction Picture | Account for the helper's side effects observed in the source tree; a dropped side effect is a finding, not a safe clearance. |
| `escaped_local_address_source_aware` | source-aware | helper/refactor | html_report, patch_corpus, evidence_manifest | code-logic-stored-pointer.md#Stored pointer / escaped-address checklist | If a patch stores \`&local\` into longer-lived state, file the stack/local lifetime bug or prove the address does not escape the current frame before it is consumed. |
| `setup_return_guard_source_aware` | source-aware | helper/refactor | html_report, patch_corpus, evidence_manifest | code-logic-setup-result.md#Setup-result / publication audit | When setup/helper status flows toward publish/register/add work, state where the return is checked or file a finding for the unchecked path before publication. |
| `newly_exposed_silent_failure_source_aware` | source-aware | helper/refactor | html_report, patch_corpus, evidence_manifest | code-logic-setup-result.md#Setup-result / publication audit | When a patch rewires a control/capability to a setter that rejects an advertised zero/default value, compare the public contract to the setter preconditions and file the newly exposed silent failure if the replay/apply path drops the return. |
